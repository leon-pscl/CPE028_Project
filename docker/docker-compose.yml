services:
  web:
    build:
      context: ../apps/web
      dockerfile: Dockerfile
    ports:
      - "5173:5173"
    volumes:
      - ../apps/web:/app
      - /app/node_modules
    env_file: ../.env.local
    command: npm run dev -- --host 0.0.0.0